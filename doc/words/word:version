name:                version
stack:               ( -- version subversion subsubversion )
return stack:        ( -- )
string stack:        ( -- )

category:            status

decription:          return version, subversion and subsubversion of currently
                     executing yoda

contexts:            stateless

standard:            no
                     standard has no equivalent
