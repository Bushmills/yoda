name:                maxint
stack:               ( -- n )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            arithmetics, system

description:         pushs the largest value of the largest signed integer to stack.
