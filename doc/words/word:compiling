name:                compiling
stack:               ( -- flag )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            compiler

decription:          returns a flag which indicates whether yoda is currently
                     interpreting or compiling

standard:            no
                     standard has no direct equivalent,
                     and would code corresponding semantics as "state @"
                     https://forth-standard.org/standard/core/STATE
