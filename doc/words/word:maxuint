name:                maxuint
stack:               ( -- u )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            arithmetics, system

decription:          largest unsigned number which can be represented with the chosen model
