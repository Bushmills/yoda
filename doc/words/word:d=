name:                $=
stack:               ( -- f )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
contexts:            stateless
category:            strings, comparison

decription:          compare two strings $1, $2. A true flag is produced when
                     strings are identical.
                     If one string is a substring of the other, strings are not
                     considered identical.
