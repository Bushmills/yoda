name:                lowercase1$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 -- $2 )
contexts:            stateless
category:            strings

description:         sets first char of string $1 to lowercase.
                     Remaining chars aren't touched.

standard:            no
deviation:           standard has no equivalent
