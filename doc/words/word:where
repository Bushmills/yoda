name:                where
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            dictionary
input stream:        word name

decription:          reads a word name from input stream, looks up its source
                     location and outputs it as dirs/file:line

standard:            no
                     standard has no equivalent

