name:                words
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            dictionary

description:         shows a listing of names in first word list of search order.

standard:            yes
