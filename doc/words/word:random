name:                random
stack:               ( -- u )
return stack:        ( -- )
string stack:        ( -- )
category:

decription:          produce a 32 bit random number

contexts:            stateless
