name:                >env$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
category:            strings, environment

decription:          write string $1 into environment variable with the name
                     specified by $2

contexts:            interpreting, compiling
