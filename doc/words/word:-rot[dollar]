name:                -rot$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 $3 -- $3 $1 $2 )
contexts:            stateless
category:            string stack

description:         moves top string stack item below the third item from top

example:             none
notes:               none

standard:            no
deviation:           standard has no equivalent
