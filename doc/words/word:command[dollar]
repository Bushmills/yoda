name:                command$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
category:            string, shell

decription:          execute $1 as shell command, passing "$2" as command line parameter(s)

contexts:            stateless
