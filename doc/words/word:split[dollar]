name:                split$
stack:               ( u -- )
return stack:        ( -- )
string stack:        ( $1 -- $2 $3 )
contexts:            stateless
category:            strings

description:         split string $1 at position u into two strings, returned as
                     $2 which are the first u chars of $1, and $3, which is the
                     rest of $1.

standard:            no
deviation:           standard has no equivalent

