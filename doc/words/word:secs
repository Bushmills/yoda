name:                secs
stack:               ( -- )
return stack:        ( -- )
string stack:        ( u -- )
contexts:            stateless
category:

description:         pauses for u seconds

standard:            no
                     standard has no equivalent
