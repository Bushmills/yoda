provides: screensize columns lines
once


: columns ( -- u )
      >s[++sp]="$COLUMNS"
;

: lines ( -- u )
      >s[++sp]="$LINES"
;

: screensize  ( -- x y )                   \ return terminal window dimensions
      columns
      lines
;
