name:                ud.r
stack:               ( ud u -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            number I/O

description:         output unsigned double number ud, respecting base, right boundary in
                     a space padded field of at least u chars wide.

standard:            no
deviation:           standard has no equivalent
