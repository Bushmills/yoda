name:                at
stack:               ( column row -- )
return stack:        ( -- )
string stack:        ( -- )
category:            I/O

decription:          move cursor and output position to column, row. 0 0 is upper
                     left corner of screen.

contexts:            stateless

notes:               This sends a terminal specific cursor control sequence, positioning
                     the cursor at column, row.
                     The sequence is determined by querying the tput terminal database.


