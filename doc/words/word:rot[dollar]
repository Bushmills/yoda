name:                rot$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 $3 -- $2 $3 $1 )
contexts:            stateless
category:            string stack

description:         rearranges string stack such that third item is rotated to top

standard:            no
deviation:           standard has no equivalent
