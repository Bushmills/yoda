\ testing does> with definition for an array
\ not called "array" because there's array already:

: arr  create allot does> + ;
10 arr myarr
see myarr

cr

: star '*' emit ;

: plural
   create ,
   does>  @ swap for
      dup >r execute r>
   next drop ;

' star plural stars
see stars

