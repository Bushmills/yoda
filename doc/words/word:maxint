name:                maxint
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            arithmetics, system

decription:          pushs the largest value of the largest signed integer to stack.
