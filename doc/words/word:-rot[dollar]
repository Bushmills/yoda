name:                -rot$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 $3 -- $3 $1 $2 )
category:            string stack

decription:          moves top string stack item below the third item from top

contexts:            stateless
example:             none
notes:               none
