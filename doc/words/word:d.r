name:                d.r
stack:               ( d u -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            number I/O

description:         output signed double number, respecting base, right boundary
                     in a space padded field of at least u chars wide.

standard:            yes
url:                 https://forth-standard.org/standard/core/DDotR
