name:                pack$
stack:               ( a u -- )
return stack:        ( -- )
string stack:        ( -- $1 )
contexts:            stateless
category:            strings

decription:          packs a c-addr type string into a string of characters which
                     is pushed to string stack.

example:             none
notes:               complementary with unpack$

standard:            no
deviation:           standard has no equivalent
