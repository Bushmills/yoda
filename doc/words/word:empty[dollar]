name:                empty$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( ??? -- )
contexts:            stateless
category:            strings

description:         reinitialise the string stack

standard:            no
deviation:           standard has no equivalent
