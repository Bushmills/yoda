name:                thisfile
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            files

decription:          produces handle of currently loading file, or 0 for console.
                     alias of source, provided for better symmetry with thisfile$

standard:            no
deviation:           standard has no equivalent
