name:                nip$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
category:            strings

decription:          remove and discard second from top string stack item

contexts:            stateless
