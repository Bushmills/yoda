name:                $@
stack:               ( u -- )
return stack:        ( -- )
string stack:        ( -- $1 )
contexts:            stateless
category:            strings

description:         push a string taken from string memory location u
                     to string stack.

notes:               complementary to $!
see also:            $, here$, allot$
