name:                satisfy$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            compiling

decription:          attempt to compile the word whose name is in top string stack item
                     from library. Failure to do so results in a "word not found" error.

standard:            no
deviation:           standard has no equivalent

