name:                convert
stack:               ( n1 -- )
return stack:        ( -- )
string stack:        ( -- $1 )
contexts:            stateless
category:            number conversion

decription:          converts signed number n1 to a string which is pushed
                     to string stack. Respects base.
