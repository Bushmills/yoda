name:                rdrop
stack:               ( -- )
return stack:        ( x -- )
string stack:        ( -- )
contexts:            stateless
category:            return stack

description:         drop top return stack item

standard:            no
deviation:           standard has no equivalent
