name:                drop$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            strings

decription:          discard top string stack item

standard:            no
                     standard has no equivalent

