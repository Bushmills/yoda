name:                over$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- $1 $2 $1 )
category:            strings

decription:          push a duplicate of second item of string stack to string stack

contexts:            stateless
