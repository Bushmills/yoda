provides: vocabulary

: vocabulary  ( -- ) ( stream: word )
    word$ dup$ newwordlist$ constant$           \ constant points wordlist name
    does> set-context                           \ that addr is also the wid
;
