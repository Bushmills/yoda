name:                >
stack:               ( n1 n2 -- flag )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            signed comparison

decription:          performs an arithmetic comparison on signed numbers n1 and n2,
                     replaces them against a true flag if n1 is greater than n2, or
                     a false flag in other cases.

standard:            yes
url:                 https://forth-standard.org/standard/core/more
