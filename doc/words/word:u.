name:                u.
stack:               ( u -- )
return stack:        ( -- )
string stack:        ( -- )
category:            number formating

decription:          output unsigned number u., respecting radix stored in base

contexts:            interpreting, compiling
