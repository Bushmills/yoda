name:                maxint
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
decription:          pushs the largest value of the largest signed integer to stack.
contexts:            stateless
