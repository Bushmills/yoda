name:                command$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
contexts:            stateless
category:            string, shell

description:         execute $1 as shell command, passing "$2" as command line parameter(s)

standard:            no
deviation:           standard has no equivalent