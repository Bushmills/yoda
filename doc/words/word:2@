name:                2@
stack:               ( a -- [a+1] [a] )
return stack:        ( -- )
string stack:        ( -- )
category:            memory

decription:          fetches contents of two memory cells at address a and a+1, and
                     returns them on stack with item at a on top.

contexts:            interpreting, compiling
example:             none
notes:               none
