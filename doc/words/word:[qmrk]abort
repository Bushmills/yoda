name:                ?abort
stack:               ( flag -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            flow control, error handling

description:         a non-zero value for flag aborts further execution and
                     returns to interactive mode.
                     if flag is zero, execution continues.

standard:            no
deviation:           standard has no equivalent
