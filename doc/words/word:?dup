name:                ?dup
stack:               ( x -- x x | 0 )
return stack:        ( -- )
string stack:        ( -- )
decription:          duplicates x if value of x isn't zero
contexts:            interpreting, compiling
