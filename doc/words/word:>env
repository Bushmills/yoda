name:                >env
stack:               ( x -- )
return stack:        ( -- )
string stack:        ( $1 -- )
category:            environment

decription:          write number x to environment variable with the name specified
                     by top string stack item

contexts:            stateless
