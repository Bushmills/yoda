name:                forth
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            dictionary

description:         the wordlist containing the majority of yoda's core words.
                     Runtime semantics are to replace the first wordlist in the
                     list of searched word lists, thereby becoming the first word
                     list searched.

standard:            yes
url:                 https://forth-standard.org/standard/search/FORTH
