name:                umin
stack:               ( u1 u2 -- u1|u2 )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            arithmetics

decription:          returns the lower of two unsigned numbers u1, u2

standard:            no
                     standard has no equivalent
