name:                lack
stack:               ( -- flag )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            dictionary, conditional compilation

input stream:        word name

decription:          looks up word name in dictionary, returns true for word not found/
                     undefined, or false if word exists.


notes:               he logical complement of "have"

standard:            no
deviation:           standard has no equivalent
