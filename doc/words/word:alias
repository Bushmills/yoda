name:                alias
stack:               ( xt -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            defining
input stream:        space delimited string

description:         create an additional header for xt semantics, for an alternative name

example:             ' lshift alias <<
