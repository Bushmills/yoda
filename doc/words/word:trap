name:                trap
stack:               ( xt -- )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            system

description:         execute xt on occurance of a signal named $1

standard:            no
deviation:           standard has no equivalent
