name:                .
stack:               ( n -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            I/O

decription:          outputs n as signed integer, with a trailing space appended.

notes:               respects contents of variable "base"

standard:            yes
url:                 https://forth-standard.org/standard/core/d
