name:                @
stack:               ( a -- x )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            memory

description:         reads from memory at address a, returns read content as x

standard:            yes
url:                 https://forth-standard.org/standard/core/Fetch
