version 0.8.2, 2022mar19,22:14
fixed prepend$ and $,
added indent$
reviewed see and codebody
added -leading$ -trailing$ trim$
is looks now through all vocabularies in order for deferred word
fixed an obscure bug with satisfy which wouldn't compile a word after defer.
streamlined wordlist creation and vocabularies
place many stack diagrams on line with defining word, which often allow to
view stack effect of a word using by using the word "p"  (print the single line
where word was defined)
words column width adapts to screen columns and longest header.
fixed a glitch in d=
solved a problem in pattern: after ctrl-c, return wouldn't return implicit return codes.
   specifying return values now.
