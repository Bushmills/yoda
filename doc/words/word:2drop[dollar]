name:                2drop$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
contexts:            stateless
category:            string stack

decription:          remove and trash the topmost two items of string stack

standard:            No
                     Standard has no equivalent
