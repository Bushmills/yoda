name:                pluck$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 $3 -- $1 $2 $3 $1 )
contexts:            stateless
category:            strings

description:         adds a copy of third string stack item on top of string stack

standard:            no
deviation:           standard has no equivalent

