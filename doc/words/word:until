name:                until
stack:               ( flag -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            compileonly
category:            flow control

description:         conclude a begin ... until flow control construct by compiling
                     code which performs a conditional jump back to begin. If flag
                     is true, the jump is not taken, and execution continues with
                     code following until.

standard:            yes
url:                 https://forth-standard.org/standard/core/UNTIL
