name:                =
stack:               ( x1 x2 -- flag )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            comparison

decription:          replaces top two stack items against true if they match,
                     or false otherwise.

standard:            yes
                     https://forth-standard.org/standard/core/Equal
