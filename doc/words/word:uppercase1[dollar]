name:                uppercase1$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 -- $2 )
contexts:            stateless
category:            strings

description:         changes first character in $1 to uppercase.
                     Remaining characters aren't affected.

standard:            no
deviation:           standard has no equivalent
