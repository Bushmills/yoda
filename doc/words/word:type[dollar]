name:                type$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 -- )
category:            strings, I/O

description:         prints top string stack item as string and removes it
                     from string stack.

contexts:            stateless

standard:            no
deviation:           standard has no equivalent
