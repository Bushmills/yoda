created:             auto     (remove line after having checked and revised description)
name:                #>$
stack:               ( u -- )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            number conversion

decription:          finalises a pictured number conversion sequence. the converted
                     number is provided as top of string stack

notes:               meant to be paired with <#
                     equivalent to    #> pack$   but much more efficient.
