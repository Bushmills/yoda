name:                have$
stack:               ( -- xt|0 )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            dictionary

description:         examines whether a word with the name of top string stack item
                     exists, and returns its execution token, or 0 if it doesn't.

standard:            no
deviation:           standard has no equivalent
