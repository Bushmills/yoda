name:                !
stack:               ( x a -- )
return stack:        ( -- )
string stack:        ( -- )
category:            memory

decription:          stores x at memory address a

contexts:            stateless
