name:                else
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            compilation

description:         a compile time flow control word
                     part of if .. else .. then flow control construct.

example:             ...
                     true if  ( taken )
                     else     ( not taken )
                     then
