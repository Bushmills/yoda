name:                join$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- $3 )
category:            strings

decription:          append top string stack item to next of top string stack item.

contexts:            interpreting, compiling
