name:                2drop$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
contexts:            stateless
category:            string stack

description:         remove and trash the topmost two items of string stack

standard:            no
deviation:           standard has no equivalent
