name:                until
stack:               ( f -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            compileonly
category:            flow control

decription:          conclude a begin ... until flow control construct by compiling
                     code which performs a conditional jump back to begin. If flag
                     f is true, the jump is not taken, and execution continues with
                     code following until.
