name:                over$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- $1 $2 $1 )
contexts:            stateless
category:            strings

description:         push a duplicate of second item of string stack to string stack

standard:            no
deviation:           standard has no equivalent

