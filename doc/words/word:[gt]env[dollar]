name:                >env$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
contexts:            stateless
category:            strings, environment

description:         write string $1 into environment variable with the name
                     specified by $2

standard:            no
deviation:           standard has no equivalent

