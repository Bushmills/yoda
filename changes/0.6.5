version 0.6.5, 2022feb14,14:08
next iteration vocabularies.
words displays multiple entries in voc list multiple times.
vocabulary, only, also, previous, forth, order, set-current, get-current, definitions appear all to be
functional. Few words remaining which should search all vocs in order search only context at this
point. No word order words. for context, set-context provided right now, mirroring
set/get-current. Renamed wordlists to search
added trap, improved compile,

