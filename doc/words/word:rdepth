name:                rdepth
stack:               ( -- u )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            return stack

description:         returns number of items on return stack

standard:            no
deviation:           standard has no equivalent
