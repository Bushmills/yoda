name:                esc
stack:               ( -- c )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            constants

description:         a constant returning the ASCII value of Escape character.

notes:               same as ^[

standard:            no
deviation:           standard has no equivalent
