name:                decimal
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:

decription:          switches radix, used for number in- and output, to 10
