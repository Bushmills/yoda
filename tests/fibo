#!/usr/local/bin/yoda

: fibo ( u1 -- u2 )
      1- dup 1 > if
         dup recurse swap
         1-  recurse +
      then ;

need . resolve
: fibos  ( u -- )
      2 do
         i fibo . cr
      loop  ;

16 fibos
see fibo
