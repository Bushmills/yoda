name:                _
stack:               ( ??? -- )
return stack:        ( -- )
string stack:        ( ??? -- )
category:            stacks

decription:          empties stack and string stack. shorthand for empty empty$

contexts:            stateless
