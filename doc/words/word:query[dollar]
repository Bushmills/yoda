name:                query$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- $1 )
contexts:            stateless
category:            I/O, strings

decription:          prompt for a line of input, store it as top string stack item
