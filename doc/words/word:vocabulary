name:                vocabulary
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
input stream:        ( word )
category:            defining, wordlists, vocabularies

description:         read a space delimited string from input, create a vocabulary with
                     the name of that string.

standard:            no
deviation:           standard has no equivalent
