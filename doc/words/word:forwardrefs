name:                forwardrefs
stack:               ( -- a )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            dictionary, compilation

decription:          produces address of a variable, used as flag. When content
                     aren't zero, use of unknown words in colon definition won't
                     result in error, but in creating a forward reference for later
                     resolving.


example:             forwardrefs on
notes:               a shortcut +f exists for "forwardrefs on"
                     a shortcut -f exists for "forwardrefs off"
