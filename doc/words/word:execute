name:                execute
stack:               ( xt -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:

decription:          removes an execution token from stack and executes the word
                     associated with it.

example:             none
notes:               to obtain the execution token of a word, see '
                     This allows treating executable code as unnamed or
                     anonymous entity, what "lambdafication" is essentially
                     about.

standard:            yes
                     https://forth-standard.org/standard/core/EXECUTE
