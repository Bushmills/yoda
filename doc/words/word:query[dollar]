name:                query$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- $1 )
contexts:            stateless
category:            I/O, strings

description:         prompt for a line of input, store it as top string stack item.
                     query$ is now a deferred word, allowing simple replacement against
                     a custom version of query$

standard:            no
deviation:           standard has no equivalent
