name:                count
stack:               ( a -- a+1 c )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            memory

decription:          reads character from a, increments a and places character on top

standard:            yes
