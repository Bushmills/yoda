name:                thisfile$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- $1 )
contexts:            stateless
category:            loading

description:         push fully qualified path and name of the currently
                     loaded file to string stack

standard:            no
deviation:           standard has no equivalent
