name:                off
stack:               ( a -- )
return stack:        ( -- )
string stack:        ( -- )
category:            memory

decription:          writes false to memory address a

contexts:            stateless
