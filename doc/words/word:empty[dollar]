name:                empty$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( ??? -- )
contexts:            stateless
category:            strings

decription:          reinitialise the string stack

standard:            no
deviation:           standard has no equivalent
