name:                2@
stack:               ( a -- [a+1] [a] )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            memory

description:         fetches contents of two memory cells at address a and a+1, and
                     returns them on stack with item from a on top.

standard:            yes
url:                 https://forth-standard.org/standard/core/TwoFetch
