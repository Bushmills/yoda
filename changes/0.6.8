version 0.6.8, 2022feb18,16:49
moved mid$ to library, renamed needed to neededanyof, added needed and needed$, renamed waive to omit
can put (multi line) sections of high level code into bash source, which will be evaluated
moved several optionals to libraries. Added push$ which compiles to better code than env, while being
more flexible. env has been redefined to make use of push$.
regrouping words to pull hilevel together into consecutive sections, categorised them (editor folds).
removed inout construct

