name:                compiling
stack:               ( -- flag )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            compiler

description:         returns a flag which indicates whether yoda is currently
                     interpreting or compiling

standard:            no
deviation:           standard has no direct equivalent,
                     and would code corresponding semantics as "state @"
url:                 https://forth-standard.org/standard/core/STATE
