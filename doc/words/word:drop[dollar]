name:                drop$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            strings

description:         discard top string stack item

standard:            no
deviation:           standard has no equivalent

