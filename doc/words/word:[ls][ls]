name:                <<
stack:               ( u1 u2 -- u3 )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            arithmetics, bit logic

description:         logical shift left of u1 by u2 positions,
                     yielding u3 as result

notes:               << is an alias of lshift
standard:            no
                     lshift is described by standard, but its
                     alias << isn't.
url:                 https://forth-standard.org/standard/core/LSHIFT
