name:                nip$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- )
contexts:            stateless
category:            strings

description:         remove and discard second from top string stack item

standard:            no
deviation:           standard has no equivalent

