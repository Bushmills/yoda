name:                msb
stack:               ( -- x )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            constants

decription:          produces a number which has only the most significant bit set.
                     This value depends on the number of bits chosen for cell width.

standard:            no
                     standard has no equivalent
