version 0.7.8, 2022mar11,12:00
can enter positive and negative double len numbers.
Decimal point can be anywhere but leading.
count of digits left of decimal point is stored in variable dpl
Currently multiple decimal points are accepted, but renders dpl meaningless.
