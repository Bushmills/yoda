name:                .r
stack:               ( n u -- )
return stack:        ( -- )
string stack:        ( -- )
category:            I/O
decription:          outputs signed number n, padded with leading spaces to u chars.
contexts:            stateless
