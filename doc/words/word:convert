name:                convert
stack:               ( n1 -- )
return stack:        ( -- )
string stack:        ( -- $1 )
contexts:            stateless
category:            number conversion

description:         converts signed number n1 to a string which is pushed
                     to string stack. Respects base.
                     Change of name is considered.

standard:            no
deviation:           standard has no equivalent
