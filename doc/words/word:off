name:                off
stack:               ( a -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            memory

description:         writes false to memory address a

standard:            no
deviation:           standard has no equivalent

