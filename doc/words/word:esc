name:                esc
stack:               ( -- c )
return stack:        ( -- )
string stack:        ( -- )
category:            constants

decription:          a constant returning the ASCII value of Escape character.

contexts:            stateless

notes:               same as ^[

