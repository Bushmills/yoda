name:                2r>
stack:               ( -- x1 x2 )
return stack:        ( x1 x2 -- )
string stack:        ( -- )
contexts:            stateless
category:            stack, return stack

decription:          moves two items from return stack to stack. Order of
                     items on stack will be identical as it was on return stack

standard:            yes
url:                 https://forth-standard.org/standard/core/TwoRfrom
