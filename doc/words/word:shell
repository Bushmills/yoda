name:                shell
stack:               ( -- a )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            system

decription:          a variable, used as flag to turn off or on passing unknown
                     words for execution to shell.


example:             shell on
notes:               a shortcut +s exists for "shell on"
                     a shortcut -s exists for "shell off"
