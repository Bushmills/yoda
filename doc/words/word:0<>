name:                0<>
stack:               ( x -- f )
return stack:        ( -- )
string stack:        ( -- )
decription:          replaces x against a false flag if x was zero, a true flag otherwise
contexts:            stateless
