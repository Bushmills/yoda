name:                $,
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            strings

decription:          writes $1 to address here$ of strings storage, then causes
                     here$ to point to the next free string storage location.

standard:            no
deviation:           standard has no equivalent
