name:                2swap$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 $3 $4 -- $3 $4 $1 $2 )
contexts:            stateless
category:            string stack

description:         swaps two pairs of string stack items

standard:            no
                     standard has no equivalent
