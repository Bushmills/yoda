name:                rot$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 $3 -- $2 $3 $1 )
contexts:            stateless
category:            string stack

decription:          rearranges string stack such that third item is rotated to top

standard:            no
                     standard has no equivalent
