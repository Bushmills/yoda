name:                neededanyof
stack:               ( -- flag )
contexts:            stateless
category:            dictionary
input stream:        whole input line

description:         splits remainder of input line into space delimited strings,
                     which are tested as word names against being forward referenced.
                     If any appears among the forward referenced, that is, needed,
                     words, the result flag is true, otherwise false.

notes:               former "needed" renamed. new needed checks single word name only now.
standard:            no
deviation:           standard has no equivalent
