name:                swap$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- $2 $1 )
contexts:            stateless
category:            string stack

decription:          swaps the top two string stack items

standard:            no
                     standard has no equivalent
