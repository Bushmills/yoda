name:                #s
stack:               ( x -- 0 )
return stack:        ( -- )
string stack:        ( $1 -- $2 )
contexts:            stateless
category:            pictured number conversion, string stack

description:         converts all (remaining) digits of x, respecting current
                     radix, to characters, which are prepended to $1


notes:               meant to be used with pictured number conversion, albeit
                     this implemenation doesn't require it. See notes at "hold"
standard:            no
deviation:           operates on single length numbers
                     change is considered.
url:                 https://forth-standard.org/standard/core/numS
