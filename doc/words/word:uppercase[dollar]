name:                uppercase$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            strings

decription:          changes case of all chars in $1 to upper

standard:            no
                     standard has no equivalent
