name:                dup$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 -- $1 $1 )
decription:          duplicate top string stack item
contexts:            interpreting, compiling
