name:                origin
stack:               ( -- u )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            file, compiling

description:         produce the file handle of the file from which source is currently
                     read and compiled from.
                     If not reading from a file, the handle of tty (0) is produced.

standard:            no
deviation:           standard has no exact equivalent,
                     but SOURCE-ID resembles origin.
url:                 https://forth-standard.org/standard/file/SOURCE-ID
