name:                waive
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            loading from file
input stream:        input read from file

decription:          disregards all text ahead until next empty line.

example:             none
notes:               useful for conditional compilation, allowing to skip
                     blocks of code.

standard:            no
deviation:           standard has no equivalent

