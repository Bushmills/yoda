name:                emit
stack:               ( c -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            I/O

decription:          takes c which represents a byte sized ASCII value of a character,
                     and output the character represented by that ASCII value.

notes:               not all control characters can be printed with emit. Safe characters
                     to output are those with ASCIIs in the range of $20 ... $7e
