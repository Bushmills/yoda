name:                version$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- $1 )
category:            status

decription:          produce a string with version number on top of string stack,
                     in the format  version.subversion.subsubversion

contexts:            interpreting, compiling
