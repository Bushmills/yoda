version 0.7.9, 2022mar13,21:03
char "  char '  [char] ' and [char] " functional  (bug fix)
dpl returns value, not address.
value of dpl changed to count of digits right of decimal dot, akin to a float mantissa.
unpack$ can now unpack string consisting of single tick char (')  (bug fix)
added 2nip
removed atoms
added 'singlequotedstrings' and 'multi word single quoted strings'
extended input number conversion to allow  $-ff $ff.ff $-ff.ff #-99 #-99.99 %-101 %-101.01
