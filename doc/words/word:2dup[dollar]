name:                2dup$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- $1 $2 $1 $2 )
contexts:            stateless
category:            string stack

description:         duplicate the top two string stack items

standard:            no
deviation:           standard has no equivalent
