name:                edit
stack:               ( u -- )
return stack:        ( -- )
string stack:        ( -- )
category:            tools

decription:          opens a source file by number in the configured editor.
                     Obtain a list of source files and their associated
                     number by executing "files"

contexts:            interpreting, compiling
