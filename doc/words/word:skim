name:                skim
stack:               ( a1 -- a2 x )
return stack:        ( -- )
string stack:        ( -- )
category:            memory

decription:          read x from memory location at address a1, then
                     increment a1 by cell to give a2

contexts:            stateless

notes:               this does for cell sized contents what count does for bytes