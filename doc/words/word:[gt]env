name:                >env
stack:               ( x -- )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            environment

description:         write number x to environment variable with the name specified
                     by top string stack item

standard:            no
deviation:           Standard has no equivalent
