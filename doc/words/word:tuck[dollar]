name:                tuck$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- $2 $1 $2 )
category:            strings

description:         insert a copy of top of string stack underneath
                     the second item of string stack

contexts:            stateless

standard:            no
deviation:           standard has no equivalent
