name:                loading
stack:               ( -- flag )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            file I/O

description:         produces a true flag when executed while loading from file.
                     when executed from console, pushed flag will be false.

standard:            no
deviation:           standard has no equivalent
