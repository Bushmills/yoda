name:                empty$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( ??? -- )
contexts:            stateless
category:            strings

decription:          reinitialise the string stack

standard:            no
                     standard has no equivalent
