name:                #files
stack:               ( -- u )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            files

decription:          produces the count of loaded files. Those are the files
                     whose names can be shown by executing "files"
