name:                on
stack:               ( a -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            memory

decription:          write true to memory address a

standard:            no
deviation:           standard has no equivalent

