name:                @
stack:               ( a -- x )
return stack:        ( -- )
string stack:        ( -- )
category:            memory

decription:          reads from memory at address a, returns read content as x

contexts:            stateless
