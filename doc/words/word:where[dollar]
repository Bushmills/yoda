name:                where$
stack:               ( -- u1 u2 )
return stack:        ( -- )
string stack:        ( $1 -- )
contexts:            stateless
category:            dictionary

description:         expects a word name as $1 on string stack, returns
                     file and line number of its source location, which
                     are returned as u1 and u2

standard:            no
deviation:           standard has no equivalent


