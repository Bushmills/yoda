name:                join$
stack:               ( -- )
return stack:        ( -- )
string stack:        ( $1 $2 -- $3 )
contexts:            stateless
category:            strings

description:         append top string stack item to next of top string stack item.

standard:            no
deviation:           standard has no equivalent

