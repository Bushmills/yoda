
exists check lest done		\ don't reload

create errors 0 ,

: check    ( -- )
        errors @ ?dup
        if . "errors"
        else  "No errors"
        then type$ cr  ;

: expected ( x1 x2 -- )
        = 1+ errors +! ;
