version 0.7.4, 2022feb28,01:19
defer and is reimplemented. is can now be compiled into words which revector deferred words.
removed atom,
updated +loop
renamed compileonly to compiler and interpretonly to interpreter
made both wordlists visible, thereby allowing controlling order of searching them
through order.

