name:                0=
stack:               ( x -- flag )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            comparison

decription:          replaces x against a true flag if x was zero, a false flag otherwise

standard:            yes
url:                 https://forth-standard.org/standard/core/ZeroEqual
