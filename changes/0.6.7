version 0.6.7, 2022feb17,21:05
detokeniser generates code for function calls from xt.
transient headers track the wordlist which was current when they there created, so that removal
can consult the proper wordlist. Highlevelised only and forth.
Factored generating function names.
left$ right$ split$ mid$ accept negative indici again.

