name:                code
stack:               ( -- )
return stack:        ( -- )
string stack:        ( -- )
contexts:            stateless
category:            compiling
input stream:        word

description:         initiate defining a code word, with name as read from input.

example:             code bye  "exit" code,   ;code
standard:            no
deviation:           standard has no equivalent
